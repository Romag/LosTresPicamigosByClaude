# Normalize line endings in the repo to LF; let Git pick working-tree endings per platform.
* text=auto

# The Maven wrapper shell script must stay LF so it runs on macOS/Linux.
mvnw text eol=lf

# Windows scripts stay CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf
